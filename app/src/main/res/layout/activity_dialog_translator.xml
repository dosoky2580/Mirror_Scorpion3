<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    android:padding="12dp">

    <EditText
        android:id="@+id/et_upper_dialog"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#1A1A1A"
        android:gravity="top"
        android:hint="النص الأصلي يظهر هنا..."
        android:padding="15dp"
        android:textColor="#FFFFFF" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp"
        android:background="#222222">
        <TextView
            android:id="@+id/tv_lower_dialog"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="15dp"
            android:textColor="#03DAC5"
            android:textSize="18sp"
            android:hint="الترجمة تظهر هنا..." />
        <ImageButton
            android:id="@+id/btn_dialog_speaker"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:src="@android:drawable/ic_lock_silent_mode_off"
            android:background="?attr/selectableItemBackgroundBorderless"/>
    </RelativeLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="15dp">
        
        <Button android:id="@+id/btn_lang_right" android:layout_width="0dp" android:layout_weight="1" android:text="العربية" android:backgroundTint="#FFD700" android:textColor="#000000"/>
        
        <ImageButton android:id="@+id/btn_swap" android:layout_width="50dp" android:layout_height="wrap_content" android:src="@android:drawable/ic_menu_sort_alphabetically" android:background="?attr/selectableItemBackgroundBorderless"/>
        
        <ImageButton android:id="@+id/btn_main_mic" android:layout_width="70dp" android:layout_height="70dp" android:src="@android:drawable/ic_btn_speak_now" android:backgroundTint="#FF0000"/>
        
        <Button android:id="@+id/btn_lang_left" android:layout_width="0dp" android:layout_weight="1" android:text="التركية" android:backgroundTint="#FFD700" android:textColor="#000000"/>
    </LinearLayout>

</LinearLayout>
